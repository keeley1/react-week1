import React from "react";
import Counter from "../components/stateExampleComponent";

/*
React state = instead of manipulating individual pieces of the
UI directly, you describe the different states that a component can be in.
*/

function State() {
    return (
        <>
        <Counter/>
        </>
    );
};

export default State;